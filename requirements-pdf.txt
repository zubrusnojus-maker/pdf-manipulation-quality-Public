# PDF processing dependencies (Quality-optimized for Nov 2025)
torch>=2.2.0
transformers>=4.44.0
accelerate>=0.20.0  # For device_map="auto" with large models
pymupdf
pdf2image
pikepdf
tqdm
sentencepiece  # Required for Llama/Mistral tokenizers
protobuf  # Required for some model configs
